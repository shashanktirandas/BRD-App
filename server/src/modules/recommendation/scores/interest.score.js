const ScoreInterface = require("../interfaces/ScoreInterface");

class InterestScore extends ScoreInterface {

    calculate(post, profile) {
        return 0;
    }

}

module.exports = InterestScore;
